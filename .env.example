# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-project-id

# Pub/Sub Topic Name
PUBSUB_TOPIC=your-topic-name

# Optional: Path to Google Cloud service account credentials JSON file
# If not set, will use default credentials (gcloud auth application-default login)
# 
# If the JSON file is in the same folder, use just the filename:
# GOOGLE_APPLICATION_CREDENTIALS=cred.json
#
# Or use a relative path from the project root:
# GOOGLE_APPLICATION_CREDENTIALS=./cred.json
#
# Or use an absolute path:
# GOOGLE_APPLICATION_CREDENTIALS=/full/path/to/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=cred.json
